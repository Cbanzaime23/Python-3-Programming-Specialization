prompt = "{} has ${}"
prompt = prompt + "\n" + "\n"
prompt = prompt + "Category: {}" + "\n"
prompt = prompt + "Phrase:  {}" + "\n"
prompt = prompt + "Guessed: {}" + "\n"
prompt = prompt + "\n"
prompt = prompt + "Guess a letter, phrase, or type 'exit' or 'pass':"
#prompt.format("name","prizeMoney", "category", "obscured_phrase", "guessed")

x = input(prompt)
